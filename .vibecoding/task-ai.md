# Task AI — Change Embedding Model to OpenAI

## Tasks
- [x] Read `task.md` (user requested to change embedding model to OpenAI).
- [x] Create an implementation plan detailing changes across `config.py`, `embeddings.py`, `index.py`, and `metrics.py`.
- [x] Wait for user confirmation (`t=true`) to proceed with implementation.
- [x] Modify `config.py` to use `text-embedding-3-small`.
- [x] Modify `embeddings.py` to use `langchain_openai.OpenAIEmbeddings`.
- [x] Update `index.py` and `metrics.py` to use the new embeddings model.
- [x] Commit changes.
